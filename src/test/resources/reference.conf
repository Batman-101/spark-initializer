#this is a common configuration file for the app, regardless of environment
# it is pulled in first and then the specific environment overrides it

configuration {
  name = "common_and_generic_stuff"
  otherthing = "OverrideMe"
}

sparkOptions {
  spark.master = "local[1]"
}

hadoopOptions {
  something = "somethingElse"
}

logLevel="ERROR"