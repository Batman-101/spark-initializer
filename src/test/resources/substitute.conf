include "reference.conf"

# This will be substituted at run time - logLevelValue, container, storageAccount will be provided at run time only
logLevel=${logLevelValue}
defaultFileSystem="abfss://"${container}"@"${storageAccount}".dfs.core.windows.net"
dummy=first
